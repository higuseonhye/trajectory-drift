# Local development — Drift & Return

See [return/docs/LOCAL.md](https://github.com/higuseonhye/return/blob/main/docs/LOCAL.md) for full stack setup.

**Drift** runs on `:3001`. **Return** on `:3000`. **Continuity** on `:3002`.

Legacy local folder names (`trajectory-native`, etc.) may still exist — rename when idle. GitHub repos are `return`, `drift`, `continuity`.
